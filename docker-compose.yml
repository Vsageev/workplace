services:
  backend:
    build:
      context: .
      dockerfile: packages/backend/Dockerfile
    container_name: workplace-backend
    restart: unless-stopped
    ports:
      - '${BACKEND_PORT:-3847}:3847'
    environment:
      NODE_ENV: development
      PORT: 3847
      HOST: 0.0.0.0
      CORS_ORIGIN: ${CORS_ORIGIN:-http://localhost:5173}
    volumes:
      - backups:/app/backups
    profiles:
      - full

  frontend:
    build:
      context: .
      dockerfile: packages/frontend/Dockerfile
    container_name: workplace-frontend
    restart: unless-stopped
    ports:
      - '${FRONTEND_PORT:-5173}:5173'
    environment:
      VITE_API_URL: http://backend:3847
    depends_on:
      - backend
    profiles:
      - full

volumes:
  backups:
